# Demo script (< 3 min)

Full setup, interviewer script, screenshots, and terminal transcript: [demo.md](demo.md).

Short path:

1. Bring up stack + seed (see demo.md).
2. Login `demo@guardrail.local` / `demopass123`.
3. Accept: `Send 5 MOCK_USDC to Alice` → Approve → show Anvil tx.
4. Reject: `Approve unlimited MOCK_USDC for 0xEvil` → `infinite_approve`.
5. Optional: Solana chip + Audit page.

Local / mock chains only. Do not point demo keys at mainnet.
